import React from "react";
import MainLog from "./MainLog";

const dataComponent = {
  title: "Join Now",
};

const Join = () => {
  return <MainLog dataComponent={dataComponent} />;
};

export default Join;
