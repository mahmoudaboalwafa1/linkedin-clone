import React from "react";
import MainLog from "./MainLog";

const dataComponent = {
  title: "Sign In",
};

const Signin = () => {
  return <MainLog dataComponent={dataComponent} />;
};

export default Signin;
